import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      alignItems: "center",
      padding: 20,
      paddingTop: 40
    },
    inputStyle: {
        fontSize: 40,
        marginTop: 10
    },
    viewDoBtn: {
        top: 60
    },

});